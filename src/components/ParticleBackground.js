import ParticleBackground from './ParticleBackground';

const PortfolioWebsite = () => {
  return (
    <div className="relative min-h-screen">
      <ParticleBackground />
      <header className="relative z-10"> {/* Existing header content */} </header>
      <main className="relative z-10"> {/* Existing main content */} </main>
      <footer className="relative z-10"> {/* Existing footer content */} </footer>
    </div>
  );
};

export default PortfolioWebsite;
